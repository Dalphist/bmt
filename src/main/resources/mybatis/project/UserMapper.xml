<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bmt.mapper.UserMapper">

	<select id="getPassword" parameterType="String"  resultType="String">
		SELECT PASSWORD FROM sys_user WHERE username = #{username}
	</select>
	
	<select id="getRole"  parameterType="String" resultType="String">
		SELECT r.role_name FROM sys_user u JOIN sys_role r ON u.role_id = r.id WHERE username = #{username}
	</select>
	
	<select id="login"  parameterType="String" resultType="String">
		SELECT r.role_name FROM sys_user u JOIN sys_role r ON u.role_id = r.id WHERE username = #{username}
	</select>
	
</mapper>